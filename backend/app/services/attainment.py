from collections.abc import Iterable


def calculate_attainment(
    scores: Iterable[float | None], threshold: float
) -> float:
    """Calculate CO attainment once score-entry behavior is implemented."""
    raise NotImplementedError(
        "Attainment calculation will be implemented with the score-entry workflow."
    )
